import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles(theme => ({
    root: {
        minHeight: '100vh',
        // background: 'url(https://mars.nasa.gov/assets/mrn/mrn_splash.jpg) no-repeat, center, center fixed',
        backgroundSize: 'cover'
    },

}))
